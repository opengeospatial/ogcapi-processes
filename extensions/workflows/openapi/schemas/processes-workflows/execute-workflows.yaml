type: object
properties:
  process:
    type: string
    format: uri-reference
    description: URI to the process execution end point (ie. /processes/{processId}/execution)
  inputs:
    additionalProperties:
      $ref: "input-workflows.yaml"
  outputs:
    additionalProperties:
      $ref: "../processes-core/output.yaml"
  subscriber:
    $ref: "../processes-core/subscriber.yaml"
