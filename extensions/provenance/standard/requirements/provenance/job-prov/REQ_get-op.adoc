[[req_provenance_job-prov_get-op]]
[requirement]
====
[%metadata]
identifier:: /req/provenance/prov-get-op

[.component,class=conditions]
--
. The parameter `{jobID}` resolves to a valid job as per <<OAProc-1,OGC API - Processes - Part 1: Core, Conformance Class 'Core'>>.
. The job reference (path: `/jobs/{jobID}`) for which provenance information is requested has completed successfully (`status: successful`).
--

[.component,class=part]
--
For every created job, the server SHALL support the HTTP GET operation on path `/jobs/{jobID}/prov`.
--

====
