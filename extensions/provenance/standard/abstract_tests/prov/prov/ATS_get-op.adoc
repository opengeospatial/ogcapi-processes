[[ats_provenance_job-prov_get-op]]

[abstract_test]
====
[%metadata]
identifier:: /conf/provenance/prov-get-op
target:: <<req_provenance_job-prov_get-op,/req/provenance/prov-get-op>>
test-purpose:: Validate that the server supports HTTP GET operation at the `/jobs/{jobID}/prov` path to retrieve job provenance
test-method::
+
--
1. Construct a path for the {root}/jobs/{jobID}/prov path with a valid job identifier.

2. Issue an HTTP GET request to the path.

3. Validate that the response status code is 200 (OK) or 406 (Not Acceptable) if content negotiation is not satisfied.

4. Validate that the response contains provenance information in the supported format.

5. Validate that the response structure respects the provenance schema if applicable.
--
====
