description: Parametrization definition of a submitted job.
content:
   application/json:
      schema:
         $ref: '../../schemas/processes-job-management/jobDefinition.yaml'
   text/html:
      schema:
         type: string
