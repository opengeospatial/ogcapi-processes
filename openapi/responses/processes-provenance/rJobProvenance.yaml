description: Provenance details of a completed job.
content:
   application/json:
      schema:
         $ref: 'https://www.w3.org/submissions/prov-json/schema'
   application/provenance+json:
      schema:
         $ref: 'https://www.w3.org/submissions/prov-json/schema'
   application/ld+json:
      schema:
         $ref: 'https://www.w3.org/submissions/prov-jsonld/schema.json'
   application/provenance+xml:
      schema:
         # https://www.w3.org/ns/prov.xsd
         type: string
   application/xml:
      schema:
         # https://www.w3.org/ns/prov.xsd
         type: string
   text/xml:
      schema:
         # https://www.w3.org/ns/prov.xsd
         type: string
   text/provenance-notation:
      schema:
         type: string
   application/n-triples:
      schema:
         type: string
   text/turtle:
      schema:
         type: string
   text/html:
      schema:
         type: string
