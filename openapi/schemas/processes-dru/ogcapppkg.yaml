type: object
required:
  - executionUnit
properties:
  processDescription:
    type: object
    required:
    - process
    properties:
      process:
        $ref: "../processes-core/process.yaml"
  executionUnit:
    $ref: "executionUnit.yaml"
