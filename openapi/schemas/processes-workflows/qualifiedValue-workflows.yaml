allOf:
  - $ref: "../processes-core/format.yaml"
  - $ref: "../processes-core/fieldsModifiers.yaml"
  - type: object
    required:
      - value
    properties:
      value:
        $ref: "inputValue-workflows.yaml"
