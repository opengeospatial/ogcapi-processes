allOf:
  - $ref: "descriptionType.yaml"
  - type: object
    required:
      - schema
    properties:
      schema:
        $ref: "https://json-schema.org/draft/2020-12/schema"
      dataClasses:
        type: array
        items:
          type: string
          format: uri
