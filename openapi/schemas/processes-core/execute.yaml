type: object
properties:
  processID:
    type: string
    format: uri
  inputs:
    additionalProperties:
      $ref: "values.yaml"
  outputs:
    additionalProperties:
      $ref: "outputSelection.yaml"
  subscriber:
    $ref: "subscriber.yaml"
