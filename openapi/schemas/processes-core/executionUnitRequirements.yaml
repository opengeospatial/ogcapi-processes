type: object
propreties:
  executionUnitRequirements:
    type: object
    properties:
      remote-access:
        type: boolean
      staging:
        type: string
        enum:
          - local-file
          - remote-access
