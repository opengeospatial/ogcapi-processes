get:
   summary: retrieve the job provenance
   description: |
     Retrieve the job provenance details.
   operationId: getJobProvenance
   tags:
     - Jobs
     - Provenance
   parameters:
     - $ref: "../../parameters/processes-core/jobID.yaml"
   responses:
     200:
       $ref: "../../responses/processes-provenance/rJobProvenance.yaml"
     404:
       $ref: "../../responses/processes-provenance/rJobProvMissing.yaml"
     406:
       $ref: "../../responses/processes-provenance/rJobProvUnsupportedFormat.yaml"
     500:
       $ref: "../../responses/common-core/rServerError.yaml"
