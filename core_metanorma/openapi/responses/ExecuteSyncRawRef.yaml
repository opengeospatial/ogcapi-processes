description: |-
  Response when negotiate synchronous execution, response=raw, transmissionMode=reference and for any number of output values.
headers:
  Link:
    schema:
      type: string
    description: One or more Link headers pointing to each raw output.
