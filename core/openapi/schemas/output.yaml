output:
   allOf:
      - $ref: 'dataType.yaml'
      - type: object
        properties:
           id:
             type: string
           transmissionMode:
              $ref: 'transmissionMode.yaml'