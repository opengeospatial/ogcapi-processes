type: object
- required:
  - href
properties:
  uom:
    type: object
    required:
      - name
    properties:
      name:
        type: string
      reference:
        type: string
        format: uri
  format:
    $ref: "format.yaml"
  href:
    type: string
    format: uri
