execute:
  type: object
  required:
    - outputs
  properties:
    inputs:
      type: array
      items:
        $ref: 'input.yaml'
    outputs:
      type: array
      items:
        $ref: 'output.yaml'