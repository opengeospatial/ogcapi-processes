additionalProperties:
  oneOf:
    - $ref: "inlineOrRefData.yaml"
    - $ref: "input.yaml"
    - type: array
      items:
        oneOf:
          - $ref: "inlineOrRefData.yaml"
          - $ref: "input.yaml"
