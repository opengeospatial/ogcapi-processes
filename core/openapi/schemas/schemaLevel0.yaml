oneOf:
  - $ref: reference.yaml
  - type: object
    properties:
      maximum:
        type: number
      exclusiveMaximum:
        type: boolean
        default: false
      minimum:
        type: number
      exclusiveMinimum:
        type: boolean
        default: false
      maxItems:
        type: integer
        minimum: 0
      minItems:
        type: integer
        minimum: 0
        default: 0
      enum:
        type: array
        items: {}
        minItems: 1
        uniqueItems: false
      type:
        type: string
        enum:
          - array
          - boolean
          - integer
          - number
          - string
      items:
        oneOf:
          - "$ref": "schemaLevel0.yaml"
          - "$ref": "reference.yaml"
      format:
        type: string
      default: {}
      nullable:
        type: boolean
        default: false
      example: {}
      contentMediaType:
        type: string
      contentEncoding:
        type: string
      contentSchema:
        $ref: "schemaLevel0.yaml"
    additionalProperties: false
