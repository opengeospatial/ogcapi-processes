allOf:
  - $ref: "descriptionType.yaml"
  - type: object
    required: [schema]
    properties:
      schema:
         type: object
      supportedCRS:
         type: array
         items:
            type: object
            properties:
              crs:
                type: string
              default:
                type: boolean
                default: false
      observedProperty:
        $ref: "observedProperty.yaml"
