allOf:
  - $ref: "descriptionType.yaml"
  - type: object
    required:
      - schema
    properties:
      schema:
        type: object
      formats:
        type: array
        items:
          $ref: format.yaml
      observedProperty:
        $ref: "observedProperty.yaml"
