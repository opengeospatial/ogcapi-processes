allOf:
   - $ref: "processSummary.yaml"
   - type: object
     properties:
        inputs:
           type: array
           items:
              $ref: "inputDescription.yaml"
        outputs:
           type: array
           items:
              $ref: "outputDescription.yaml"
