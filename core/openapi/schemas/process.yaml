allOf:
   - $ref: "processSummary.yaml"
   - type: object
     properties:
        inputs:
          additionalProperties:
              $ref: "dataDescription.yaml"
        outputs:
          additionalProperties:
              $ref: "dataDescription.yaml"
