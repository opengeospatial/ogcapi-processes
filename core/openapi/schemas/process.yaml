allOf:
   - $ref: "processSummary.yaml"
   - type: object
     properties:
        inputs:
           type: object
           patternProperties:
              "^.*$":
                 $ref: "inputDescription.yaml"
        outputs:
           type: object
           patternProperties:
              "^.*$":
                 $ref: "inputDescription.yaml"
