allOf:
  - $ref: "descriptionType.yaml"
  - type: object
    required:
      - schema
    properties:
      schema:
        oneOf:
          - $ref: schemaLevel0.yaml
          - $ref: schemaLevel1.yaml
          - $ref: schemaLevel2.yaml
          - $ref: schemaLevel3.yaml
