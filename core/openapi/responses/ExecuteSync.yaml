description: Result of synchronous execution
content:
  /*:
    schema:
      oneOf:
        - type: string
        - type: number
        - type: integer
        - type: object
        - type: array
        - type: boolean
        - type: null
        - type: string
          format: binary
        - $ref: "../schemas/results.yaml"
          description: mode=sync,response=document,any txMode and no. outputs
