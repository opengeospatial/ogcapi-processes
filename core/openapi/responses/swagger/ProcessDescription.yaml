description: A process description.
content:
  application/json:
    schema:
      $ref: "../../schemas/swagger/process.yaml"
    examples:
      processDecription:
        summary: A Process Description example
        value:
          type: object
          $ref: "../../../examples/json/ProcessDescription.json"
